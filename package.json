{
  "name": "my-app",
  "version": "0.0.1",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "build:lib": "ng build ngx-pagination-andy",
    "link:lib": "cd dist/ngx-pagination-andy && npm link",
    "link:app": "npm link ngx-pagination-andy",
    "unlink:lib": "cd dist/ngx-pagination-andy && npm unlink && npm unlink ngx-pagination-andy && npm install",
    "dev:lib": "npm run build:lib && npm run link:lib && npm run link:app",
    "buildlib": "ng-packagr -p ng-package.json",
    "release:patch": "npm version patch && npm run buildlib && npm publish --access public",
    "release:minor": "npm version minor && npm run buildlib && npm publish --access public",
    "release:major": "npm version major && npm run buildlib && npm publish --access public",
    "build-lib": "npm run update-version && ng build ngx-pagination-andy && npm run publish",
    "update-version":"cd projects/ngx-pagination-andy && npm version patch && cd ../../",
    "publish": "cd dist/ngx-pagination-andy && npm publish --access public"
  },
  "private": true,
  "dependencies": {
    "@angular/common": "^20.0.0",
    "@angular/compiler": "^20.0.0",
    "@angular/core": "^20.0.0",
    "@angular/forms": "^20.0.0",
    "@angular/platform-browser": "^20.0.0",
    "@angular/router": "^20.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^20.0.3",
    "@angular/cli": "^20.0.3",
    "@angular/compiler-cli": "^20.0.0",
    "@types/jasmine": "~5.1.0",
    "jasmine-core": "~5.7.0",
    "karma": "~6.4.0",
    "karma-chrome-launcher": "~3.2.0",
    "karma-coverage": "~2.2.0",
    "karma-jasmine": "~5.1.0",
    "karma-jasmine-html-reporter": "~2.1.0",
    "ng-packagr": "^20.1.0",
    "typescript": "~5.8.2",
    "wait-on": "^8.0.4"
  }
}
